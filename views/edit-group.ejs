<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Study Group</title>
</head>
<body>
    <h2>Edit Study Group</h2>
    <form action="/study-group/<%= group._id %>/edit" method="POST" enctype="multipart/form-data">
        <label>Group Name:</label>
        <input type="text" name="groupName" value="<%= group.groupName %>">

        <label>Subjects:</label>
        <input type="text" name="subjects" value="<%= group.subjects.join(', ') %>">

        <label>Capacity:</label>
        <input type="number" name="capacity" value="<%= group.capacity %>">

        <label>Description:</label>
        <textarea name="description"><%= group.description %></textarea>

        <label>Availability:</label>
        <input type="text" name="availability" value="<%= group.availability.join(', ') %>">

        <label>Password (optional):</label>
        <input type="password" name="password">

        <label>Profile Picture:</label>
        <input type="file" name="groupProfilePicture">

        <button type="submit">Update Group</button>
    </form>

    <!-- Delete Button -->
    <form action="/study-group/<%= group._id %>/delete" method="POST" style="margin-top: 20px;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this group?')">Delete Group</button>
    </form>
</body>
</html>
